<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockadmin.selection.mapper.Stock60MinPoolMapper">

    <sql id="BaseColumns">
        SELECT code AS stock_code,
               stock_name AS stock_name,
               market_code AS market_code,
               NULL AS signal_name,
               NULL AS org_num
        FROM t_stock_pool
    </sql>

    <select id="selectBySignalName" resultType="com.stockadmin.selection.domain.Stock60MinPoolEntry">
        <include refid="BaseColumns"/>
        WHERE stock_type = 'stock'
          AND code IS NOT NULL
          AND TRIM(code) &lt;&gt; ''
          AND UPPER(COALESCE(stock_code, '')) NOT LIKE '%.BJ'
        ORDER BY code
    </select>

    <select id="selectBySignalNameAndStockCodes" resultType="com.stockadmin.selection.domain.Stock60MinPoolEntry">
        <include refid="BaseColumns"/>
        WHERE stock_type = 'stock'
          AND code IS NOT NULL
          AND TRIM(code) &lt;&gt; ''
          AND UPPER(COALESCE(stock_code, '')) NOT LIKE '%.BJ'
          AND code IN
        <foreach collection="stockCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
        ORDER BY code
    </select>

</mapper>
