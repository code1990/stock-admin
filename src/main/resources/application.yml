spring:
  application:
    name: stock-admin
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: ${STOCK_ADMIN_DB_URL:jdbc:sqlite:/root/data/stock_stat.db}
    username: ${STOCK_ADMIN_DB_USERNAME:}
    password: ${STOCK_ADMIN_DB_PASSWORD:}

server:
  port: 18080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.stockadmin: INFO

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.stockadmin.selection.domain
  configuration:
    map-underscore-to-camel-case: true

stock-admin:
  quote-60:
    datasource:
      driver-class-name: org.sqlite.JDBC
      url: ${STOCK_ADMIN_QUOTE_60_DB_URL:jdbc:sqlite:/root/data/stock_quote_60_cnfin.db}
  selection:
    default-limit: 100
